language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"

# command to install dependencies
install:
  - "pip install ."
  - "pip install -e .[dev,test]"

# command to run tests
script: nosetests

addons:
  apt_packages:
    - pandoc
#    - libblas-dev
#    - python-dev